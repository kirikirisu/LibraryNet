mutation createLibrary($input: LibraryInput!) {
  createLibrary(input: $input) {
    library {
      id
      title
      description
      icon
    }
    errors {
      field
      message
    }
  }
}
